// Задача 1:
// Сделайте файл-модуль math.js, в котором будет лежать функция square, 
// возводящая число в квадрат. В файле index.js экспортируйте модуль math.js, и с его
//  помощью возведите число в квадрат.

let f = require('./a');

console.log(f.square(2));
